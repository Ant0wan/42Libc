language: c

os:
    - linux
    - osx

sudo: false

addons: *gcc7

compiler:
    - clang
    - gcc

script:
    - make
    - make clean

env:
    global:
        - MAKEFLAGS="-j"

branches:
    only:
        - master
